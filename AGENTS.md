## Agent skills

### Issue tracker

Issues live as GitHub Issues on `AndreasJosef/agera` (personal fork; the team's GitLab origin is no longer active). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), used as literal label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: root `CONTEXT-MAP.md` plus one `CONTEXT.md` per app/package. See `docs/agents/domain.md`.
